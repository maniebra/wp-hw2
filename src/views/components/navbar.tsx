import React from 'react';

export default function Navbar() {
  return (
    <header
      style={{
        borderRadius: "1rem",
        padding: '0.5rem',
        textAlign: 'center',
      }}
    >
      Paint-Line SPA
    </header>
  );
}
